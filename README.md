
I make software fast by understanding what the hardware is actually doing.

Lately that's meant RL environments that put frontier LLMs to work writing CPU/GPU/TPU kernels — verifiable rewards, noise-controlled timing, a Rust sandbox for hostile agent code — and a from-scratch CUDA path tracer up to 12× faster than the reference implementation. Before that: cluster observability for Tenstorrent's AI accelerators, and seastar-rs, the Rust bridge to ScyllaDB's Seastar framework.

Open to performance engineering (including ML), low-level & distributed systems, observability infrastructure, and RL-environment work. Warsaw-based (CET) — open to remote; contract or full-time.
